export  class Participant {
  constructor(public nom: string, public prenom: string, public mail: string,
              public preferences: string, public allergies: string, public idProposition: string ) {
  }
}
